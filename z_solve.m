% Матричный метод решения
% на вход:
%   Ybus — матрица узловых проводимостей
%   I_inj — вектор инъекций токов
% на выход:
%   U — вектор узловых напряжений
function U = matrix_solve(Ybus, I_inj)
  % метод 1: использование оператора \
  U = Ybus \ I_inj;
  % метод 2: явное обращение (не рекомендуется для больших матриц)
  % Zbus = inv(Ybus);
  % U = Zbus * I_inj;
end
